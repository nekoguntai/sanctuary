/**
 * Layout Module
 *
 * Main application layout with sidebar navigation, mobile menu,
 * and about/version modal.
 */

export { Layout } from './Layout';
export type { LayoutProps } from './types';
