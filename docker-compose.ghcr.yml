# =============================================
# Sanctuary Bitcoin Wallet - Docker Compose (GHCR)
# =============================================
# Usage: Pull pre-built images from GitHub Container Registry
#   docker-compose -f docker-compose.ghcr.yml up -d
#
# Note: Set GITHUB_REPOSITORY to your repo (e.g., nekoguntai/sanctuary)
# =============================================

services:
  # ============================================
  # PostgreSQL Database
  # ============================================
  postgres:
    image: postgres:16-alpine
    container_name: sanctuary-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-sanctuary}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sanctuary}
      POSTGRES_DB: ${POSTGRES_DB:-sanctuary}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sanctuary} -d ${POSTGRES_DB:-sanctuary}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - sanctuary-network

  # ============================================
  # Redis (required for worker + backend coordination)
  # ============================================
  redis:
    image: redis:7-alpine
    container_name: sanctuary-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - sanctuary-network

  # ============================================
  # Backend API Server (from GHCR)
  # ============================================
  backend:
    image: ghcr.io/${GITHUB_REPOSITORY:-nekoguntai/sanctuary}-backend:${IMAGE_TAG:-latest}
    container_name: sanctuary-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      worker:
        condition: service_healthy
    environment:
      # Server
      NODE_ENV: ${NODE_ENV:-production}
      PORT: 3001
      API_URL: ${API_URL:-http://localhost:3001}
      CLIENT_URL: ${CLIENT_URL:-http://localhost}

      # Database
      DATABASE_URL: postgresql://${POSTGRES_USER:-sanctuary}:${POSTGRES_PASSWORD:-sanctuary}@postgres:5432/${POSTGRES_DB:-sanctuary}?schema=public

      # Redis (required)
      REDIS_URL: redis://redis:6379

      # JWT
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}

      # Encryption and internal auth (required)
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required (at least 32 chars)}
      ENCRYPTION_SALT: ${ENCRYPTION_SALT:-sanctuary-node-config}
      GATEWAY_SECRET: ${GATEWAY_SECRET:?GATEWAY_SECRET is required}

      # Worker heartbeat (required)
      WORKER_HEALTH_URL: ${WORKER_HEALTH_URL:-http://worker:3002/health}

      # Bitcoin Network
      BITCOIN_NETWORK: ${BITCOIN_NETWORK:-mainnet}

      # Electrum Server
      ELECTRUM_HOST: ${ELECTRUM_HOST:-electrum.blockstream.info}
      ELECTRUM_PORT: ${ELECTRUM_PORT:-50002}
      ELECTRUM_PROTOCOL: ${ELECTRUM_PROTOCOL:-ssl}

      # Price APIs
      MEMPOOL_API: ${MEMPOOL_API:-https://mempool.space/api/v1}
      COINGECKO_API: ${COINGECKO_API:-https://api.coingecko.com/api/v3}
      KRAKEN_API: ${KRAKEN_API:-https://api.kraken.com/0/public}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - sanctuary-network

  # ============================================
  # Background Worker (from GHCR)
  # ============================================
  worker:
    image: ghcr.io/${GITHUB_REPOSITORY:-nekoguntai/sanctuary}-backend:${IMAGE_TAG:-latest}
    container_name: sanctuary-worker
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      LOG_LEVEL: ${LOG_LEVEL:-info}

      DATABASE_URL: postgresql://${POSTGRES_USER:-sanctuary}:${POSTGRES_PASSWORD:-sanctuary}@postgres:5432/${POSTGRES_DB:-sanctuary}?schema=public
      REDIS_URL: redis://redis:6379

      WORKER_HEALTH_PORT: 3002
      WORKER_CONCURRENCY: ${WORKER_CONCURRENCY:-5}

      BITCOIN_NETWORK: ${BITCOIN_NETWORK:-mainnet}
      ELECTRUM_HOST: ${ELECTRUM_HOST:-electrum.blockstream.info}
      ELECTRUM_PORT: ${ELECTRUM_PORT:-50002}
      ELECTRUM_PROTOCOL: ${ELECTRUM_PROTOCOL:-ssl}

      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required (at least 32 chars)}
      ENCRYPTION_SALT: ${ENCRYPTION_SALT:-sanctuary-node-config}
      GATEWAY_SECRET: ${GATEWAY_SECRET:?GATEWAY_SECRET is required}
    command: ["node", "dist/app/src/worker.js"]
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - sanctuary-network

  # ============================================
  # Database Migrations & Seeding (one-time run)
  # ============================================
  migrate:
    image: ghcr.io/${GITHUB_REPOSITORY:-nekoguntai/sanctuary}-backend:${IMAGE_TAG:-latest}
    container_name: sanctuary-migrate
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-sanctuary}:${POSTGRES_PASSWORD:-sanctuary}@postgres:5432/${POSTGRES_DB:-sanctuary}?schema=public
    command: ["sh", "-c", "npx prisma migrate deploy && npx prisma db seed"]
    networks:
      - sanctuary-network
    restart: "no"

  # ============================================
  # Frontend (Nginx + React) (from GHCR)
  # ============================================
  frontend:
    image: ghcr.io/${GITHUB_REPOSITORY:-nekoguntai/sanctuary}-frontend:${IMAGE_TAG:-latest}
    container_name: sanctuary-frontend
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    environment:
      BACKEND_HOST: backend
      BACKEND_PORT: 3001
    ports:
      - "${FRONTEND_PORT:-80}:80"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - sanctuary-network

# ============================================
# Networks
# ============================================
networks:
  sanctuary-network:
    driver: bridge

# ============================================
# Volumes
# ============================================
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
