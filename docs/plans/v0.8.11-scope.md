# v0.8.11 Scope Tracker

Date: 2026-03-03
Status: Open
Owner: TBD

## Goal

Turn post-release observations from `v0.8.10` into a concrete `v0.8.11` sprint scope with clear acceptance criteria.

## Priority Workstreams

1. Release hygiene follow-through
- Confirm `v0.8.10` release object state (`isDraft=false`, `isPrerelease=false`).
- Confirm release assets and install instructions are present.
- Confirm Umbrel package metadata reflects `v0.8.10` and updated image digests.

2. CI and test confidence hardening
- Monitor `quick-backend-integration-smoke` runtime and flake rate for 1 week.
- Keep targeted integration runner behavior (`scripts/run-integration-tests.sh`) stable in CI and local use.
- Add one regression test for argument passthrough behavior in integration runner scripts.

3. Coverage quality guardrails
- Preserve backend events notification branch coverage at 100% in gateway coverage runs.
- Add a lightweight coverage delta check in CI docs/process so drops are detected quickly.

## Acceptance Criteria

- `v0.8.10` release page verified and marked healthy.
- A tracked issue exists for each workstream above.
- CI remains green on `main` for 3 consecutive days with no smoke-lane regressions.

## Execution Checklist

- [ ] Create GitHub tracking issue: `Release follow-up: v0.8.11 scope`
- [ ] Link this plan from that issue
- [ ] Assign owners and target dates
- [ ] Create child issues for each workstream
- [ ] Review progress in next weekly maintenance sync

## Ready Command (when GitHub API is reachable)

```bash
gh issue create \
  --title "Release follow-up: v0.8.11 scope" \
  --body-file docs/plans/v0.8.11-scope.md
```
